# DuoFocus backend — environment template
# Copy to .env and fill in real values. NEVER commit the real .env.

# Phase 1 uses no environment variables yet.
# Future phases (auth, Firebase, database) will register their variables here, e.g.:
# FIREBASE_API_KEY=
# FIREBASE_PROJECT_ID=
